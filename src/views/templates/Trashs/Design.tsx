import styled from "styled-components";

export const Container = styled.section`
  display: grid;
  grid-row-gap: 24px;
  padding: 36px 0;
  grid-template-columns: 1fr;
  
  & > div {
    width: 100%;
  }
`
